# ReliefWeb AI Platform — merged requirements
# Install: pip install -r requirements.txt

# Web framework
flask>=3.0.0
flask-cors>=4.0.0

# Production WSGI server
gunicorn>=21.2.0

# HTTP
requests>=2.28.0
httpx>=0.24.0

# Optional S3-compatible Cloudflare R2 backup uploads
boto3>=1.34.0

# PDF processing
pypdf>=4.0.0
PyMuPDF>=1.24.0

# DOCX processing (for reference document uploads)
python-docx>=1.1.0

# LangChain / LangGraph (agent)
langchain>=0.1.0
reportlab>=4.0.0
langchain-core>=0.1.0
langchain-ollama>=0.1.0
langchain-openai>=0.1.0
langgraph>=0.1.0

# Vector store
chromadb>=0.5.0

# Embeddings (chromadb default)
sentence-transformers>=2.3.0

# Environment
python-dotenv>=1.0.0

# Math tool (reliefweb agent)
sympy>=1.12.0

# SITREP pipeline — clustering & NLP
numpy>=1.26.0
scikit-learn>=1.4.0
umap-learn>=0.5.6
hdbscan>=0.8.38

# Agent — web search tool (optional, for web_search toggle)
duckduckgo-search>=6.0
langchain-community>=0.0.38

# Firebase authentication
firebase-admin>=6.0

# MCP (Model Context Protocol) — for arxiv-mcp-server and future MCP tools
langchain-mcp-adapters>=0.3.0
mcp>=1.0.0
flask-compress
flask-limiter>=3.5
