# Development / security scanning dependencies
# Install: pip install -r requirements-dev.txt

# Linting + formatting
ruff>=0.4.0

# Security scanners
bandit>=1.7.0
safety>=3.0.0
pip-audit>=2.7.0

# Test framework (already in requirements.txt but pinned here for dev)
pytest>=8.0.0
pytest-cov>=5.0.0